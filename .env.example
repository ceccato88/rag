# =============================================================================
# CONFIGURAÇÃO DO SISTEMA RAG
# =============================================================================

# -----------------------------------------------------------------------------
# API KEYS (OBRIGATÓRIAS)
# -----------------------------------------------------------------------------
# Chave da API OpenAI - usada para GPT-4 (geração de respostas e re-ranking)
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Chave da API Voyage AI - usada para gerar embeddings multimodais (texto + imagem)
# Obtenha em: https://www.voyageai.com/
VOYAGE_API_KEY=your_voyage_api_key_here

# -----------------------------------------------------------------------------
# CONFIGURAÇÃO DO BANCO DE DADOS ASTRA DB (OBRIGATÓRIA)
# -----------------------------------------------------------------------------
# Endpoint do seu database Astra DB - encontre no painel do DataStax
ASTRA_DB_API_ENDPOINT=https://your-database-id-region.apps.astra.datastax.com

# Token de aplicação do Astra DB - gere no painel do DataStax
ASTRA_DB_APPLICATION_TOKEN=AstraCS:your_token_here

# -----------------------------------------------------------------------------
# PROCESSAMENTO DE DOCUMENTOS
# -----------------------------------------------------------------------------
# URL do PDF a ser processado - pode ser uma URL ou caminho local
PDF_URL=https://arxiv.org/pdf/2501.13956

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES OPCIONAIS (valores padrão serão usados se não definidos)
# -----------------------------------------------------------------------------

# Diretório onde as imagens das páginas do PDF serão salvas
IMAGE_DIR=pdf_images

# Nome da collection no Astra DB onde os documentos serão armazenados
COLLECTION_NAME=pdf_documents

# Modelo do OpenAI a ser usado para geração de respostas
LLM_MODEL=gpt-4o

# -----------------------------------------------------------------------------
# PARÂMETROS DE BUSCA E GERAÇÃO
# -----------------------------------------------------------------------------
# Número máximo de candidatos retornados na busca por similaridade
MAX_CANDIDATES=5

# Máximo de tokens para o re-ranking de candidatos
MAX_TOKENS_RERANK=512

# Máximo de tokens para geração da resposta final
MAX_TOKENS_ANSWER=2048

# Máximo de tokens para transformação de queries
MAX_TOKENS_QUERY_TRANSFORM=150

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE EMBEDDING
# -----------------------------------------------------------------------------
# Dimensão dos embeddings do Voyage AI (não altere)
VOYAGE_EMBEDDING_DIM=1024

# Máximo de tokens por entrada (texto + imagem) no Voyage AI
MAX_TOKENS_PER_INPUT=32000

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE PERFORMANCE
# -----------------------------------------------------------------------------
# Número de requisições simultâneas para geração de embeddings
CONCURRENCY=5

# Tamanho do lote para inserção no banco de dados
BATCH_SIZE=100

# Timeout em segundos para download de PDFs
DOWNLOAD_TIMEOUT=30

# Tamanho do chunk para download de PDFs (bytes)
DOWNLOAD_CHUNK_SIZE=8192

# Escala do pixmap para extração de imagens (2 = alta qualidade)
PIXMAP_SCALE=2

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DO SISTEMA MULTI-AGENTE
# -----------------------------------------------------------------------------
# Número máximo de subagentes para decomposição de queries
MAX_SUBAGENTS=3

# Execução paralela de subagentes (true/false)
PARALLEL_EXECUTION=true

# Usar decomposição inteligente de queries com LLM (true/false)
USE_LLM_DECOMPOSITION=true

# Máximo de tokens para decomposição de queries
MAX_TOKENS_DECOMPOSITION=1000

# Timeout em segundos para execução de subagentes
SUBAGENT_TIMEOUT=180

# Modelo OpenAI para o sistema multi-agente
MULTIAGENT_MODEL=gpt-4o-mini
