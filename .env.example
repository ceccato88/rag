# =============================================================================
# VARIÁVEIS DE AMBIENTE SISTEMA RAG MULTI-AGENTE
# =============================================================================

# -----------------------------------------------------------------------------
# 🔑 API KEYS (OBRIGATÓRIAS)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=sk-proj-your_openai_key_here
VOYAGE_API_KEY=pa-your_voyage_key_here
ASTRA_DB_API_ENDPOINT=https://your-db-id.apps.astra.datastax.com
ASTRA_DB_APPLICATION_TOKEN=AstraCS:your_token_here

# -----------------------------------------------------------------------------
# 🤖 MODELOS DE IA (CONFIGURAÇÃO HIERÁRQUICA)
# -----------------------------------------------------------------------------
# Subagentes usam gpt-4.1-mini para eficiência
OPENAI_MODEL=gpt-4.1-mini

# Coordenador usa gpt-4.1 para qualidade superior na síntese
COORDINATOR_MODEL=gpt-4.1

# Embeddings multimodais
EMBEDDING_MODEL=voyage-multimodal-3

# Configurações dos modelos
MAX_TOKENS=4000
TEMPERATURE=0.1

# -----------------------------------------------------------------------------
# 🔧 CONFIGURAÇÕES RAG
# -----------------------------------------------------------------------------
MAX_CANDIDATES=3
COLLECTION_NAME=pdf_documents
TOP_K=5
CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# -----------------------------------------------------------------------------
# 🔒 SEGURANÇA API
# -----------------------------------------------------------------------------
API_BEARER_TOKEN=your_secure_bearer_token_here
ENABLE_RATE_LIMITING=true
RATE_LIMIT=100/minute
ENABLE_CORS=false

# -----------------------------------------------------------------------------
# 🚀 PERFORMANCE
# -----------------------------------------------------------------------------
# Multi-agent execution
MAX_SUBAGENTS=3
PARALLEL_EXECUTION=true
USE_LLM_DECOMPOSITION=true

# Timeouts (em segundos)
MULTIAGENT_TIMEOUT=300
SUBAGENT_TIMEOUT=120
REQUEST_TIMEOUT=60

# Cache settings
EMBEDDING_CACHE_SIZE=1000
EMBEDDING_CACHE_TTL=3600
RESPONSE_CACHE_SIZE=200
RESPONSE_CACHE_TTL=1800

# -----------------------------------------------------------------------------
# 📊 LOGGING & MONITORING
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
ENABLE_STRUCTURED_LOGGING=true
ENABLE_PERFORMANCE_METRICS=true
PRODUCTION_MODE=false

# Log rotation
MAX_LOG_FILE_SIZE=50
LOG_ROTATION_COUNT=5

# -----------------------------------------------------------------------------
# 🖥️ SERVIDOR
# -----------------------------------------------------------------------------
API_PORT=8000
API_WORKERS=1
HOST=0.0.0.0
RELOAD=true

# Health check
HEALTH_CHECK_INTERVAL=30
HEALTH_CHECK_TIMEOUT=10

# -----------------------------------------------------------------------------
# 📁 ARQUIVOS E DIRETÓRIOS
# -----------------------------------------------------------------------------
# Diretórios de dados
DATA_DIR=data
PDF_IMAGES_DIR=pdf_images
LOGS_DIR=logs

# Upload limits
MAX_REQUEST_SIZE=16777216  # 16MB
MAX_PDF_SIZE=52428800      # 50MB

# -----------------------------------------------------------------------------
# 🐳 DOCKER (OPCIONAL)
# -----------------------------------------------------------------------------
DOCKER_IMAGE=rag-multiagent
DOCKER_TAG=latest
COMPOSE_PROJECT_NAME=rag-system

# -----------------------------------------------------------------------------
# 🧪 DESENVOLVIMENTO (OPCIONAL)
# -----------------------------------------------------------------------------
DEBUG_MODE=false
ENABLE_DEBUG_LOGS=false
ENABLE_TEST_ENDPOINTS=false
MOCK_AI_RESPONSES=false

# Pytest configurations
PYTEST_TIMEOUT=300
TEST_COLLECTION_NAME=test_collection